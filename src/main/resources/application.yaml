spring:
  application:
    name: Appli
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5433/gamehub
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
        format_sql: true

